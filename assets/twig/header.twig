<header>
   <!-- MENU MOBILE -->
                {% if not auth %}
                <div class="col-xs-12 hidden-sm hidden-md hidden-lg mobileMenu-top">
                    <!--NAVIGATION-->
                    <nav id="navigation" class="navigation"> 
                        <div {% if menu_fixed %}data-spy="affix" data-offset-top="260" style="z-index:10000"{% endif %}>
                            <a id="menu" tabindex="-1"></a>
                            <div class="dv-navigation">{{ menu|raw }}</div>
                            <div class="clearfix"></div>
                        </div>
                    </nav>           
                </div>              
                {% endif %}
<div class=" hidden-sm hidden-lg hidden-md">
    <button class="menuMobile buttonMenuMobile fa fa-bars" data-toggle="collapse" data-target=".navbar-collapse"></button>
</div>
    <div class="row">
        <div class="col-md-4 col-md-offset-2">
            {% for i,row in position1 %}
                {{ row|raw }}
            {% endfor %}
        </div>
        <div class="col-md-4 hidden-sm hidden-xs">
            {% for i,row in position2 %}
                {{ row|raw }}
            {% endfor %}
        </div>
    </div>
    <!-- NAVBAR-->
    <div class="row">
        <nav class="navbar navbar-default navbar-section-top hidden-xs">
            <div class="col-xs-12 hidden-md hidden-sm">
            </div>
                <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="navbar-section-top-margTop">
                <div class="col-md-4 col-md-offset-2 col-sm-6 navbar-section-top-left">
                       <div class="dv-navigation">{{ menu|raw }}</div>
                        <div class="clearfix"></div>
                  <!--   <ul class="nav navbar-nav">
                        <li class="active nav-item"><a href="#">START</a></li>
                        <li class="nav-item"><a href="#">OFERTA</a></li>
                        <li class="nav-item"><a href="#">KONTAKT</a></li>
                    </ul> -->

                </div>
                <div class="col-md-4 col-sm-6  hidden-xs">
                    <form class="navbar-form navbar-right">
                       <div class="form-group">
                         <input type="text" class="form-control" placeholder="Search">
                       </div>
                       <button type="submit" class="btn btn-default">Submit</button>
                    </form>
                </div>
            </div>
        </nav>
    </div>
</header>